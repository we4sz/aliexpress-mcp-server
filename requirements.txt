# mcp 2.0.0 removed `mcp.server.fastmcp` entirely, which this server is built on.
# Unpinned, a fresh install resolves to 2.0 and the server fails to import.
mcp>=1.0,<2
httpx>=0.27.0
beautifulsoup4>=4.12.0
